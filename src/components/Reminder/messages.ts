import { defineMessages } from 'react-intl'

export default defineMessages({
  // TODO: replace it w/ an expression for end-users
  inputIsNotBetweenLowerBoundAndUpperBound: '{ input } is not between { lowerBound } and { upperBound }.',
})
