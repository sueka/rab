import { defineMessages } from 'react-intl'

export default defineMessages({
  // TODO: replace it w/ an expression for end-users
  inputIsNotBetweenLowerBoundAndUpperBound: {
    id: 'src.components.Reminder.inputIsNotBetweenLowerBoundAndUpperBound',
    defaultMessage: '{ input } is not between { lowerBound } and { upperBound }.',
  },
})
