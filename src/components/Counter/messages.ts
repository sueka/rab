import { defineMessages } from 'react-intl'

export default defineMessages({
  reset: 'reset',
  increment: '+',
  decrement: '-',
  incrementIfOdd: '+ if odd',
  willIncrementInOneSecond: '+ in 1 second',
})
